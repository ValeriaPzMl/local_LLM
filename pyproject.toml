[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "computahmind"
version = "0.1.0"
description = "Local AI coding agent"
requires-python = ">=3.12"

[project.scripts]
computah = "src.cli:main"